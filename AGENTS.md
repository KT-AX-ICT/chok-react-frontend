# Project Agent Instructions

Before working in this repository, check whether `AGENTS.local.md` exists. If it exists, read it first and treat it as the local source of machine-specific paths and personal instructions.

This is the React frontend for CHOK. Follow the existing Vite, React, and TypeScript structure, and keep UI changes consistent with the current component patterns under `src/components`.

Do not commit or upload local instruction files such as `AGENTS.local.md` or `CLAUDE.local.md`, or local-only workspace folders such as `.local/`.
